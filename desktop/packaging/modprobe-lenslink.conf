# LensLink virtual camera: one device, /dev/video9, labelled
# "LensLink Virtual Camera". exclusive_caps=1 makes the capture node
# appear only while a writer (lenslinkd) is streaming.
options v4l2loopback devices=1 video_nr=9 card_label="LensLink Virtual Camera" exclusive_caps=1
