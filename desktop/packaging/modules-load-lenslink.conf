# Creates the virtual camera device at boot: /dev/video9, "LensLink
# Virtual Camera". exclusive_caps=1 makes the capture node appear only
# while lenslinkd is streaming, so apps see the camera come and go.
v4l2loopback
